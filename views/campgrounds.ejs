<% include partials/header %>

    <h1>will be Campgrounds!</h1>

    <a href="/campgrounds/new">Add New Campground</a>

    <div>
        <% campgrounds.forEach(function(campground){ %>
            <div>
                <h4>
                    <%= campground.name %>
                </h4>
                <img src="<%= campground.image %>">
            </div>
            <% }); %>
    </div>

    <a href="/">Home</a>

    <% include partials/footer %>